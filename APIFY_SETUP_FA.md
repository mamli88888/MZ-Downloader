# راهنمای اتصال Apify به MZ Downloader

این نسخه، لینک‌های **عمومی** YouTube و Instagram را ابتدا به Apify می‌سپارد؛ اما برخلاف نسخه قبلی، پیش از انتخاب کاربر هیچ Actorی اجرا نمی‌شود. ربات همان منوی دکمه‌ای سابق را نمایش می‌دهد و پس از انتخاب کیفیت یا «فقط صدا»، درخواست انتخاب‌شده را اجرا می‌کند. اگر همه توکن‌ها یا Actorها خطا بدهند، مسیرهای قبلی ربات، شامل سایت‌ها و بات‌های تلگرام، بدون تغییر به‌عنوان fallback ادامه می‌یابند.

| پلتفرم | Actor اصلی | گزینه‌های دکمه‌ای |
|---|---|---|
| YouTube | [`streamers/youtube-video-downloader`](https://apify.com/streamers/youtube-video-downloader) | 144p تا 2160p و فقط صدا (MP3) |
| Instagram | [`apify/instagram-scraper`](https://apify.com/apify/instagram-scraper) | ویدیوی کیفیت اصلی و فقط صدا (MP3) |

برای Instagram، Actor کیفیت‌های جایگزین تولید نمی‌کند؛ بنابراین دکمه ویدیو، همان کیفیت اصلی رسانه عمومی را تحویل می‌دهد. گزینه MP3 فایل ویدیویی را پس از دریافت با `ffmpeg` به صدا تبدیل می‌کند؛ Dockerfile پروژه از قبل ffmpeg را نصب می‌کند.

## ۱. ساخت Tokenها

در Apify Console به **Settings → Integrations** بروید و یک یا چند API token بسازید. Tokenها را فقط در Variables سرویس استقرار مانند Railway یا Replit قرار دهید. هرگز آن‌ها را در GitHub، فایل `.env.example` یا پیام‌ها ثبت نکنید.

## ۲. متغیرهای محیطی

برای چند توکن، متغیر زیر را با مقادیر جداشده با کاما اضافه کنید:

```text
APIFY_ENABLED=true
APIFY_TOKENS=token-اول,token-دوم,token-سوم
APIFY_RUN_TIMEOUT_SECONDS=360
APIFY_POLL_INTERVAL_SECONDS=3
APIFY_TOKEN_COOLDOWN_SECONDS=600
```

ربات توکن‌ها را به‌ترتیب چرخشی استفاده می‌کند. اگر اجرای Actor یا API برای یک توکن خطا دهد، آن توکن بلافاصله برای مدت `APIFY_TOKEN_COOLDOWN_SECONDS` کنار گذاشته می‌شود و همان درخواست با توکن بعدی امتحان می‌شود. مقدار پیش‌فرض cooldown ده دقیقه است؛ اگر همه توکن‌ها در cooldown باشند، یک دور دیگر از آن‌ها برای جلوگیری از توقف کامل سرویس آزموده می‌شود.

> متغیر قدیمی `APIFY_TOKEN` هنوز برای یک توکن پشتیبانی می‌شود؛ اما `APIFY_TOKENS` روش پیشنهادی است. `APIFY_YOUTUBE_QUALITY` دیگر لازم نیست و نباید تنظیم شود، چون کاربر کیفیت را با دکمه انتخاب می‌کند.

## ۳. رفتار ربات پس از Deploy

در YouTube، ربات پس از انتخاب دکمه، Actor را با `videos` و `storeInKVStore=true` اجرا می‌کند و `preferredQuality` یا `preferredFormat=mp3` را از همان انتخاب کاربر می‌سازد. سپس URL فایل آماده‌شده را از dataset می‌گیرد، فایل را در پوشه موقت دانلود می‌کند و با مسیر ارسال فعلی به تلگرام می‌فرستد.

در Instagram، ربات URL عمومی پست یا Reel را با `directUrls` به Actor می‌دهد. لینک‌های `videoUrl`، `displayUrl` و رسانه‌های carousel از dataset خوانده می‌شوند. گزینه صوتی برای هر فایل ویدیویی خروجی، MP3 تولید می‌کند؛ پست تصویری صرفاً با دکمه ویدیو/کیفیت اصلی قابل دریافت است.

اگر Apify خروجی رسانه ندهد، اجرای Actor ناموفق شود، زمان اجرا از timeout عبور کند یا همه توکن‌ها شکست بخورند، ربات مسیرهای پیشین خود را ادامه می‌دهد. فایل‌های بزرگ نیز همچنان تابع `MAX_FILE_SIZE_MB`، `MAX_DOWNLOAD_SIZE_MB` و مسیر Pixeldrain/Gofile/Worker موجود در پروژه هستند.

## ۴. آزمون پیشنهادی پس از استقرار

یک لینک YouTube عمومی و یک Instagram Reel عمومی ارسال کنید. ابتدا باید منوی کیفیت نمایش داده شود. یک کیفیت و سپس گزینه «فقط صدا» را آزمایش کنید. در لاگ، عبارت `Apify gateway enabled (N token(s))` باید دیده شود. برای آزمایش failover، موقتاً یک Token نامعتبر را ابتدای `APIFY_TOKENS` بگذارید؛ ربات باید آن را cooldown کند و با Token بعدی ادامه دهد.

## ۵. نکات استفاده مسئولانه

این اتصال فقط برای محتوای عمومی‌ای است که کاربر مجوز دریافت یا استفاده از آن را دارد. مدل قیمت‌گذاری Actorها، سهمیه‌ها و محدودیت‌های پلتفرم‌های مبدأ ممکن است تغییر کنند؛ پیش از استفاده تولیدی، وضعیت آن‌ها را در Apify Console بررسی کنید.
