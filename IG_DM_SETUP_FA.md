# پل دایرکت اینستاگرام → تلگرام (ig_dm.py)

با این قابلیت، یک **پیج اینستاگرام عمومی** می‌سازی؛ کاربران هر ریلز / پست /
استوری (هر چیزی که ربات الآن پشتیبانی می‌کند) را به **دایرکت همان پیج**
می‌فرستند (لینک، یا Share خودِ پست داخل اپ اینستاگرام) و ربات همان را با
**متدهای فعلی** (AHM7 → Apify → تلگرام‌بات‌های کمکی → yt-dlp) دانلود می‌کند و
**توی تلگرامِ خود کاربر** می‌فرستد.

---

## جریان کار (چطور کاربر وصل می‌شود؟)

```
کاربر توی تلگرام:  /link
        │
        ▼
ربات یک کد ۶ رقمی می‌دهد (اعتبار ۱۵ دقیقه)      مثلاً  K7QM2P
        │
        ▼
کاربر همان کد را به دایرکت پیج اینستاگرام می‌فرستد
        │
        ▼
اتصال برقرار شد ✅   (حساب اینستاگرام ↔ چت تلگرام)
        │
        ▼
از این به بعد: هر ریلز/پست/استوری که به دایرکت پیج بفرستد
→ دانلود با همان موتور فعلی ربات → تحویل فایل در تلگرام 📥
```

- کاربر می‌تواند به‌جای لینک، خود پست/ریلز را از داخل اپ اینستاگرام با **Share → دایرکت پیج** بفرستد (media_share / clip / xma_share / reel_share پشتیبانی می‌شوند).
- `/unlink` هم برای قطع اتصال اضافه شده است.
- هیچ رفتار قبلی ربات تغییر نکرده؛ این ماژول کاملاً جداست و اگر فعال نباشد، ربات دقیقاً مثل قبل کار می‌کند.

---

## ⚠️ دربارهٔ «بدون session»

- **دانلود محتوا: بدون سشن ✅** — دقیقاً همان زنجیرهٔ فعلی ربات استفاده می‌شود و هیچ لاگین اینستاگرامی لازم ندارد.
- **خواندن دایرکت: بدون لاگین ممکن نیست ❌** — اینستاگرام هیچ API رسمی/ناشناسی برای خواندن DM نمی‌دهد. پس فقط همین ماژول با **حساب خود پیج** یک بار لاگین می‌کند و سشن را در فایل `ig_session.json` ذخیره و بعداً بازیابی می‌کند (لاگین تکراری انجام نمی‌شود). رمز پیج فقط در متغیرهای محیطی سرور خودت می‌ماند.

---

---

## 🔧 رفع مشکل لاگین روی Railway (بدون رمز!) — روش sessionid

**علامت در لاگ Railway:**

```
POST accounts/login/ → 400
CAA login fallback failed: 429 Too Many Requests
ig-dm: login failed (BadPassword): ...
```

**چرا؟** لاگینِ رمزی اینستاگرام از **IP دیتاسنتر Railway** (و حتی از IPهای VPN)
به‌احتمال خیلی زیاد رد می‌شود؛ اینستاگرام IP کلود را باور نمی‌کند و حتی با رمز
درست `BadPassword` می‌دهد و تلاش بعدی `429` می‌شود. این رفتار ربات نیست؛
محافظت خود اینستاگرام است.

**راه‌حل — لاگین فقط با کوکی sessionid مرورگر (بدون رمز عبور):**

مرورگر خودت که پیج را در آن باز می‌کنی یک سشن معتبر و قابل‌اعتماد دارد؛
ربات از همان سشن استفاده می‌کند و دیگر هیچ درخواست لاگین رمزی زده نمی‌شود:

1. در کامپیوتر، مرورگر را باز کن → `instagram.com` → مطمئن شو پیج‌ات لوگین است.
2. دکمه **F12** را بزن → تب **Application** (فایرفاکس: Storage) → منوی چپ
   **Cookies** → `https://www.instagram.com`
3. از لیست کوکی‌ها، مقدار (Value) ردیف **sessionid** را کامل کپی کن
   (رشتهٔ خیلی طولانی، وسطش `%3A` دارد).
4. در Railway → Variables این متغیر را بساز و Deploy بزن:

   ```
   IG_SESSIONID=<همان مقدار کپی‌شده>
   ```

5. در لاگ باید ببینی:

   ```
   ig-dm: enabled (page=@..., auth=sessionid, ...)
   ig-dm: login ok (session=sessionid, ...)
   ```

نکته‌ها:
- با `IG_SESSIONID` دیگر نیازی به `IG_USERNAME` / `IG_PASSWORD` نیست (بمانندشان، ضرری ندارد).
- اگر روزی اتصال قطع شد (رمز عوض کردی یا Log out زدی)، یک sessionid تازه بردار و همان متغیر را آپدیت کن.
- امنیت: sessionid مثل کلید ورود پیج است؛ فقط در Railway خودت بگذارش و با کسی به اشتراک نگذار.
- اگر مرورگر/ترمینالت مستقیم به اینستاگرام وصل نمی‌شود، اسکریپت کمکی همین کار را با پراکسی انجام می‌دهد:

  ```
  pip install "instagrapi==2.18.18" pysocks
  python ig_session_helper.py        # گزینهٔ 1 → sessionid
  ```

**روش‌های جایگزین (اگر sessionid به هر دلیلی جواب نداد):**

- **سشن کامل از IP خانگی:** `python ig_session_helper.py` → خروجی base64 → متغیر `IG_SESSION_B64`.
- **پراکسی مسکونی/موبایل** در `IG_PROXY` (مثل `socks5://user:pass@host:port`)؛ پراکسی دیتاسنتری معمولاً فایده ندارد.
- **Railway Volume** برای ماندگاری فایل سشن (پیشرفته): Volume در مسیر `/data` + متغیر `IG_DM_SESSION_FILE=/data/ig_session.json` (اولین ورود همچنان به روش‌های بالا نیاز دارد).
- اگر یک‌بار ورود موفق شد، با دستور ادمینِ `/igsession` سشن سالم فعلی را بگیر و در `IG_SESSION_B64` بگذار.
- اینستاگرام گاهی «چالش تأیید» فعال می‌کند: یک‌بار با اپ/مرورگر وارد پیج شو، تأیید را کامل کن، بعد دوباره امتحان کن.

---

## ❗ خطای «Conflict: terminated by other getUpdates» یعنی چه؟

این خطا ربطی به پل دایرکت ندارد؛ یعنی **یک نمونهٔ دیگر از همین ربات با همان توکن
هم‌زمان روشن است** (دپلوی قدیمی Railway که هنوز Service فعالش مانده، یک Service
دوم در همان پروژه، یا رباتی که روی سیستم خودت باز مانده). هر دو نمونه برای گرفتن
پیام‌ها سر و کله می‌زنند و پیام‌ها بین‌شان گم می‌شود. راه‌حل: همهٔ Service/پروسه‌های
دیگر را خاموش کن و فقط یک نمونه فعال بماند.

💡 ضمناً اگر در لاگ `Unknown AI_PROVIDER 'huggingface|cohere|mistral|off'` دیدی،
یعنی متغیر `AI_PROVIDER` عیناً همان متنِ راهنما مقدار داده شده؛ این متغیر را پاک کن
(یا یکی از مقدارهای واقعی مثل `prexzy` بگذار) — پیش‌فرض خودش درست کار می‌کند.

---

## ❗ «پست رو با Share فرستادم ولی ربات هیچ جوابی نداد»

این مشکل مخصوص نسخه‌های قدیمی بود: اینستاگرام وقتی کاربری ریلز/پست را با
Share می‌فرستد، نوع پیام را `xma_clip` / `xma_media_share` می‌گذارد و محتوایش
را در ساختار XMA (به‌صورت مدل `MediaXma` در instagrapi 2.x) قرار می‌دهد؛
نسخه‌های قبلی فقط dictهای خام را اسکن می‌کردند و پیام را **بی‌صدا** رد می‌کردند.

در نسخهٔ فعلی:
- همهٔ ساختارهای XMA (`xma_clip`, `xma_media_share`, `generic_xma`, `raw_xma`) و
  مدل‌های `Media` / `MediaXma` / `MessageLink` پشتیبانی می‌شوند؛
- اگر به هر دلیلی لینک از داخل اشتراک درنیاید، ربات دیگر بی‌صدا نمی‌ماند و
  پیام «لطفاً Copy link بزن و لینک را متن بفرست» می‌دهد؛
- در لاگ هم علت مشخص می‌شود: `extracted N url(s)` یا `no url in message (type=…)`.

اگر باز پیامی بی‌جواب ماند، در لاگ دنبال `no url in message (type=…)` بگرد و
`type` را همراه لاگ گزارش کن.

---

## 👥 فالو‌بک خودکار + پیام خوش‌آمد (نسخهٔ ۲ — فیکس «فالو‌بک نمی‌دهد»)

وقتی کسی پیج را فالو کند، ربات در اولین بررسی (حدود ۹۰ ثانیه بعد) او را
فالو‌بک می‌کند، وضعیت فالو را چک و تایید می‌کند و بلافاصله یک پیام
خوش‌آمد به دایرکتش می‌فرستد. از همان سشن پل دایرکت استفاده می‌شود —
لاگین دومی در کار نیست و هیچ قابلیت قبلی را تغییر نمی‌دهد.

**در نسخهٔ ۲ چه چیزی درست شد؟ (اگر قبلاً کار نمی‌کرد، دلیلش این‌ها بود)**
- 🔁 **فالوورهای قبلی هم فالو‌بک می‌شوند (Backfill)**: قبلاً فقط
  فالوورهایِ بعد-از-راه‌اندازی فالو‌بک می‌شدند و فالوورهای فعلی هرگز.
  حالا ربات بعد از ثبت اولیه، هر دور تا `IG_FOLLOWBACK_MAX_PER_ROUND` نفر
  (پیش‌فرض ۵) از فالوورهای فعلیِ فالو‌بک‌نشده را — با همان سقف‌های امن —
  فالو‌بک می‌کند تا لیست کم‌کم کامل شود.
- 🔁 **Unfollow → Refollow الان دیده می‌شود**: قبلاً اگر کسی unfollow و
  دوباره follow می‌کرد، ربات متوجه نمی‌شد (حافظه پاک نمی‌شد). الان
  unfollow واقعی تشخیص داده می‌شود (چک friendship) و refollow مثل یک
  فالوور جدید فالو‌بک + پیام می‌گیرد.
- ✅ **پیام خوش‌آمد فقط بعد از فالوی موفق**: قبلاً حتی اگر فالو خطا می‌داد
  پیام می‌رفت؛ الان اول فالو تایید می‌شود (چک friendship)، بعد پیام.
- ✅ **فالوی خطاخورده گم نمی‌شود**: تا ۳ بار در دورهای بعدی تلاش مجدد
  می‌شود (قبلاً یک بار خطا = برای همیشه رها).
- 🔍 **دستور تشخیصی جدید**: در تلگرام `/igfollowcheck` بزن تا وضعیت زنده
  را ببینی — پل روشن است؟ چند فالوور جدید؟ چند نفر منتظر فالو‌بک؟
- 📋 **لاگ خلاصه در پایان هر دور**: در لاگ Railway دنبال
  `ig-follow: poll — …` باش تا ببینی ربات دقیقاً چه می‌کند.

**نکته‌های مهم:**
- **دور اول فقط ثبت می‌کند** (baseline)؛ از دور بعد فالو‌بکِ جدیدها +
  Backfill شروع می‌شود — این تاخیر عمدی است تا ربات با شروع، انبوه فالو
  نکند و بلاک نشوی.
- سقف امن دارد: پیش‌فرض ۴۰ فالو در ساعت و ۱۵۰ در روز با فاصلهٔ ۵ ثانیه
  بین هر کدام (با `IG_FOLLOWBACK_HOURLY_CAP` و… قابل تغییر است). پس اگر
  فالوورهای قدیمی زیاد باشند، Backfill چند روز طول می‌کشد — طبیعی است.
- اگر پیام خوش‌آمد ارسال نشود (مثلاً دایرکتش بسته باشد)، چند بار دیگر تلاش
  می‌شود و بعد رها می‌شود.
- خاموش کردن: در Railway متغیر `IG_FOLLOWBACK_ENABLED=0` بساز و Deploy بزن.
- فقط فالو‌بکِ فالوورهای جدید (بدون Backfill): `IG_FOLLOWBACK_BACKFILL=0`.
- برای انتقال وضعیت بین دپلوی‌ها (تا کار دوباره از صفر شروع نشود)، در
  تلگرام دستور `/igfollowstate` را بزن و خروجی base64 را در متغیر
  `IG_FOLLOW_STATE_B64` بگذار (به‌ویژه اگر Volume نداری).

---

## راه‌اندازی

### ۱) متغیرهای محیطی (Railway Variables یا فایل `.env`)

| متغیر | لازم؟ | توضیح |
|---|---|---|
| `IG_USERNAME` | ✅ | یوزرنیم پیج اینستاگرام (با @ نباشد) |
| `IG_PASSWORD` | ✅ | رمز عبور پیج |
| `IG_TOTP_SECRET` | ➖ | اگر 2FA از نوع Authentication App روشن است، سکرت TOTP را بگذار تا لاگین خودکار شود |
| `IG_DM_ENABLED` | ➖ | `0` بگذاری حتی با وجود یوزر/رمز، پل خاموش می‌ماند |
| `IG_DM_PAGE_HINT` | ➖ | یوزرنیمی که در پیام `/link` به کاربر نشان داده می‌شود (پیش‌فرض: `IG_USERNAME`) |
| `IG_DM_POLL_SECONDS` | ➖ | فاصلهٔ چک کردن دایرکت (پیش‌فرض `10`) |
| `IG_DM_MAX_THREADS` | ➖ | تعداد چت‌های آخر که هر دور بررسی می‌شود (پیش‌فرض `20`) |
| `IG_DM_SESSION_FILE` | ➖ | مسیر فایل سشن (پیش‌فرض `ig_session.json` کنار ربات) |
| `IG_SESSIONID` | ⭐ پیشنهادی | کوکی `sessionid` از مرورگر خودت (F12 → Application → Cookies → instagram.com) — ورود بدون رمز، حتی روی Railway |
| `IG_PROXY` | ➖ | پراکسی مخصوص اینستاگرام مثل `socks5://user:pass@host:port` یا `http://...` |
| `IG_SESSION_B64` | ➖ | سشن کامل به‌صورت base64 (خروجی `ig_session_helper.py`) |
| `IG_FOLLOWBACK_ENABLED` | ➖ | فالو‌بک خودکار + پیام خوش‌آمد (پیش‌فرض روشن؛ `0` = خاموش) |
| `IG_FOLLOW_POLL_SECONDS` | ➖ | فاصلهٔ چک کردن فالوورهای جدید (پیش‌فرض `90`) |
| `IG_FOLLOWER_SAMPLE` | ➖ | چند فالوور آخر هر دور بررسی شود (پیش‌فرض `200`، جدیدترین‌ها اول) |
| `IG_FOLLOWBACK_HOURLY_CAP` | ➖ | حداکثر فالو‌بک در هر ساعت (پیش‌فرض `40`) |
| `IG_FOLLOWBACK_DAILY_CAP` | ➖ | حداکثر فالو‌بک در هر روز (پیش‌فرض `150`) |
| `IG_FOLLOWBACK_COOLDOWN` | ➖ | فاصلهٔ بین هر فالو‌بک به ثانیه (پیش‌فرض `5`) |
| `IG_FOLLOWBACK_DM_TEXT` | ➖ | متن پیام خوش‌آمد دایرکت (پیش‌فرض: متن فارسی ربات) |
| `IG_FOLLOWBACK_BACKFILL` | ➖ | فالو‌بکِ فالوورهای فعلیِ جاافتاده (پیش‌فرض `1` روشن و آرام؛ `0` = فقط فالوورهای جدید) |
| `IG_FOLLOWBACK_MAX_PER_ROUND` | ➖ | حداکثر Backfill در هر دور پولینگ (پیش‌فرض `5`) |
| `IG_FOLLOWBACK_NOTIFY` | ➖ | اعلان تلگرامی «✅ فالو‌بک شد» برای فالوورهای جدید (پیش‌فرض `1`) |
| `IG_FOLLOWBACK_VERIFY` | ➖ | بعد از فالو، وضعیت friendship چک و در لاگ تایید شود (پیش‌فرض `1`) |
| `IG_FOLLOW_STATE_B64` | ➖ | وضعیت فالو‌بک به‌صورت base64 (خروجی `/igfollowstate`) برای انتقال بین دپلوی‌ها |

پل وقتی فعال می‌شود که `IG_SESSIONID` تنظیم شده باشد **یا** هر دوِ `IG_USERNAME` و `IG_PASSWORD` موجود باشند؛ در غیر این صورت خاموش است و `/link` پیام «غیرفعال» می‌دهد.

### ۲) تنظیمات خود پیج اینستاگرام (مهم!)

1. پیج را **Professional/Creator** کن (اختیاری ولی توصیه می‌شود).
2. `Settings and privacy → Messages and story replies → Message controls` را طوری تنظیم کن که **همه** بتوانند به دایرکت پیام بدهند (Allow access to messages: **Everyone**)، وگرنه پیام کاربران جدید به‌عنوان Request می‌آید و قابل پردازش نیست.
3. پیج نباید Private باشد (پیج عمومی).

### ۳) وابستگی جدید

```
pip install -r requirements.txt
```

(فقط `instagrapi==2.18.18` اضافه شده است.)

### ۴) اجرا

کافی است ربات را مثل قبل ری‌استارت کنی. در لاگ باید این را ببینی:

```
ig-dm: bridge started (poll=10s, threads=20, page=@yourpage)
ig-dm: logged in as @yourpage (pk=...)
```

اگر لاگین شکست بخورد (مثلاً اینستاگرام چالش تأیید بخواهد)، خطای واضح در لاگ می‌بینی و به `BOT_ADMIN_CHAT_ID` هم پیام می‌رود. در آن حالت یک بار با مرورگر وارد پیج شو، تأیید را بزن و ربات را دوباره اجرا کن؛ سشن بعدی ذخیره می‌شود.

---

## نکات امنیتی و پایداری

- سشن پیج در `ig_session.json` نگه‌داری می‌شود؛ **این فایل را عمومی نکن**.
- اگر رمز پیج را عوض کردی، سشن خودبه‌خود باطل می‌شود و ربات با رمز جدید دوباره لاگین می‌کند.
- پل روی خطاهای اینستاگرام backoff نمایی دارد (تا ۱۰ دقیقه) و کل ربات را نمی‌بُرد.
- محدودیت نرخ: مثل خود ربات، هر کاربر/چت محدودیت `RATE_LIMIT_REQUESTS` دارد و در هر لحظه فقط یک دانلود فعال برای هر کاربر.
- در Railway فایل‌های `ig_session.json` و `ig_links.json` با هر redeploy پاک می‌شوند (اتصال‌ها یک بار دیگر باید زده شود). اگر Volume داری، پروژه را روی Volume بگذار تا ماندگار شوند.

## محدودیت‌ها (صادقانه)

- **استوری**: فقط اگر زنجیرهٔ فعلی ربات لینک استوری را دانلود کند، اینجا هم کار می‌کند؛ پل فقط URL را می‌سازد و به همان زنجیره می‌دهد.
- اینستاگرام ممکن است برای حساب‌های تازه‌ساخت‌شده محدودیت (Action Block) بگذارد؛ بهتر است پیج چند روزه باشد و بلافاصله زیر بار سنگین نرود.
- خواندن دایرکت با API غیررسمی است؛ همیشه ریسک محدودشدن حساب پیج وجود دارد. برای خدمت عمومی، حساب اختصاصی در نظر بگیر.

## فایل‌های این فیچر

| فایل | وضعیت |
|---|---|
| `ig_dm.py` | **جدید** — کل منطق پل (پایش دایرکت، اتصال کد، تحویل به موتور دانلود فعلی) |
| `bot.py` | **تغییر کوچک** — import محافظت‌شده، ۲ دستور `/link` و `/unlink`، اجرای تسک در `post_init` و قطع آن در `post_shutdown` |
| `requirements.txt` | **تغییر** — افزودن `instagrapi==2.1.2` |
| `Dockerfile` | **تغییر** — افزودن `ig_dm.py` به لیست COPY |
