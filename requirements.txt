python-telegram-bot[socks]==22.8
Telethon==1.44.0
PySocks==1.7.1
python-socks==2.8.2
python-dotenv==1.2.2
httpx[socks]==0.28.1
Pillow==12.3.0
# yt-dlp is used by:
#   - youtube_search.py for /music search and for estimating file sizes
#     shown next to quality buttons (both have fallbacks).
#   - social_gateway.py for SoundCloud / Instagram / Pinterest / Twitter /
#     Facebook / generic downloads.
# YouTube DOWNLOADS do NOT use yt-dlp — they go through the
# youtube_sites_gateway (loader.to scraper) instead.
yt-dlp==2026.7.4
# curl_cffi enables yt-dlp's browser impersonation (Chrome), which
# SoundCloud, Pinterest, Twitter, Facebook, and (with cookies) Instagram
# require to dodge bot detection. Without it, yt-dlp falls back to plain
# requests and may get blocked.
curl_cffi==0.15.0
shazamio==0.8.1
pycryptodome==3.23.0
httpx[http2]
# ig_dm.py — Instagram DM → Telegram bridge. Only used when IG_USERNAME /
# IG_PASSWORD are configured; the rest of the bot never touches it.
instagrapi==2.2.0

