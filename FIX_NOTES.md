# 📝 یادداشت تغییرات — متد جدید CreatorCrawl برای `/profile`

## 🔁 به‌روزرسانی ۶ — رفع باگ «\u200c» در جواب‌های هوش مصنوعی + راهنمای جامع دستیار

**۱. حروف درهم مثل `\u200c` در پاسخ‌های هوش مصنوعی (دستیار `/ask` و دکمه «🤖 خلاصه کن») درست شد:**

* **مشکل:** اندپوینت prexzyapis یونیکد را **دوبار** escape می‌کند؛ بعد از parse
  شدن JSON، نویسهٔ نیم‌فاصله به شکل متنِ خالیِ `\u200c` داخل جمله می‌ماند و
  کاربر چیزی مثل «تجربه\u200cای» می‌دید. همین اتفاق برای «»، <، > و بقیهٔ
  نویسه‌های خاص هم می‌افتاد (و حتی باعث می‌شد پاک‌سازی برچسب‌های داخلی
  Gemini مثل `<Section>` اصلاً اجرا نشود).
* **راه‌حل:** قبل از پاک‌سازی، همهٔ دنباله‌های متنی `\uXXXX` به نویسهٔ واقعی
  تبدیل می‌شوند (نیم‌فاصله، گیومه و…)؛ `\n` و `\/` باقی‌مانده هم تمیز می‌شوند.
  تست زنده: خلاصه‌ها حالا نیم‌فاصلهٔ درست دارند.

**۲. دستیار `/ask` حالا راهنمای کامل و جامع ربات را می‌بیند:**

* راهنمای قبلی فقط چند خط خلاصه بود؛ نسخهٔ جدید **همه‌چیز** را پوشش می‌دهد:
  - نحوه استفاده در چت خصوصی و گروه (/dl، ریپلای، منشن، ادمین‌کردن ربات)
  - الزام عضویت در کانال
  - جزئیات تک‌تک پلتفرم‌ها (اینستاگرام/آلبوم کروسل/پروفایل، یوتیوب/زیرنویس/4K،
    تیک‌تاک، توییتر/X، فیسبوک، اسپاتیفای/ZIP، ساوندکلاد، پینترست، VK)
  - توضیح تک‌تک دستورها با مثال (/search /song /caption /profile /bookmarks
    /mystats /autoshare /schedule با بازه‌های مجاز و سقف ۲۰ زمان‌بندی /ask
    /cancel /status /platforms /stats)
  - رفتارهای فنی: کش، آپلود ابری فایل‌های بزرگ، حجم تقریبی کنار دکمه‌ها، صف دانلود
  - سقف پاسخِ راهنما در پرامپت از ۳۰۰۰ به ۶۰۰۰ کاراکتر رسید تا هیچ بخشی حذف نشود.

**۳. دیگر سوال‌های کلی توسط جواب‌های آمادهٔ بی‌ربط «قهقره‌ای» نمی‌شوند:**

* قبلاً کلمه‌های عمومی مثل «کانال» یا «کیفیت» مستقیم جوابِ آمادهٔ
  احتمالاً بی‌ربط را برمی‌گرداندند و دستیار هوش مصنوعی اصلاً فرصت پاسخ
  پیدا نمی‌کرد (مثلاً سوال «عضویت در کانال اجباریه؟» جواب /autoshare
  می‌گرفت!).
* حالا فقط تطابق قوی (امتیاز ≥ ۸) جواب آماده فوری می‌دهد؛ بقیهٔ سوال‌ها به
  دستیار هوشمند با راهنمای کامل می‌روند. اگر AI در دسترس نبود، جواب آمادهٔ
  نزدیک‌تر به‌عنوان آخرین راه استفاده می‌شود.

**فایل‌های این به‌روزرسانی:** `ai_service.py`، `user_features.py`

---

## 🔁 به‌روزرسانی ۵ — دستیار هوشمند Gemini رایگان + رفع باگ /broadcast

**۱. `/ask` و خلاصه‌سازی حالا از Gemini رایگان prexzyapis استفاده می‌کنن:**

* اندپوینت: `https://prexzyapis.com/ai/gemini` (مستندات: `https://docs.prexzyapis.com`)
* **بدون نیاز به هیچ API KEY** — سرویس کاملاً رایگانه. مقدار `AI_PROVIDER=prexzy`
  (پیش‌فرض جدید؛ حذف Variable هم همین نتیجه رو داره).
* همه‌ی قابلیت‌های هوش مصنوعی یک‌جا سوییچ شدن:
  - دستور `/ask`
  - دکمه‌ی «🤖 خلاصه کن» (خلاصه‌ی فارسی کپشن‌ها و محتوای اینستاگرام/توییتر)
  - پیشنهاد هشتگ
* هوشمندسازی‌های فنی:
  - هر درخواست یک `session_id` یکتا می‌گیره تا تاریخچه‌ی مکالمات بین کاربرها
    مخلوط نشه؛
  - User-Agent مرورگر ست می‌شه (سرور درخواست‌های پایتونی خام رو 403 می‌کنه)؛
  - اگر Gemini برچسب‌های داخلی مثل `<Section>` یا `<TextBox>` رو تو جواب
    جا بذاره، قبل از ارسال پاک‌سازی می‌شن؛
  - اگر پیش‌تر `AI_API_KEY` و HuggingFace/Cohere/Mistral تنظیم کرده بودی،
    همون‌ها هم همچنان با `AI_PROVIDER=huggingface|cohere|mistral` کار می‌کنن.

**۲. رفع باگ «/broadcast داخل پیام می‌مونه»:**

* **مشکل:** وقتی ادمین به‌جای تایپ متن، روی یک پیام ریپلای می‌کرد و
  `/broadcast` می‌زد — اگه اون پیامِ هدف خودش با `/broadcast` شروع می‌شد
  (مثلاً یک پیش‌نویس قدیمی دستور، یا عکسی که کپشنش `/broadcast متن` بود)،
  پیش‌وند دستور **حذف نمی‌شد** و متن کامل (خودِ `/broadcast`) برای همه‌ی
  کاربران ارسال می‌شد.
* **راه‌حل:** پیشوند `/broadcast` (و `/broadcast@BotName`) حالا در **هر دو مسیر**
  حذف می‌شه:
  - متنِ پیامِ ریپلای‌شده؛
  - کپشنِ ارثیِ رسانه‌ها هنگام `copy_message` (فقط وقتی واقعاً تغییر کرده تا
    فرمت اصلی کپشن دست‌نخورده بمونه).
* کپشن‌های عادی (بدون پیش‌وند دستور) دقیقاً مثل قبل — بدون هیچ تغییری — ارسال
  می‌شن.

**فایل‌های این به‌روزرسانی:** `ai_service.py`، `user_features.py`، `bot.py`، `.env.example`

---

## 🔁 به‌روزرسانی ۴ — پست‌های کروسل (چند اسلایدی) درست دانلود می‌شن

**مشکل:** برای لینک‌هایی مثل `https://www.instagram.com/p/XXXX/` که پست **کروسل
(چند عکس)** هستن، ربات فقط **دکمه ویدیو** نشون می‌داد و با زدنش هم **فقط اسلاید اول
به شکل ویدیو** ارسال می‌شد — نه همه عکس‌ها.

**علت:** لینک‌های کروسل معمولاً پارامتر `img_index` ندارن؛ پس ربات نمی‌تونست از روی URL
بفهمه پست کروسله یا ویدیو و پست رو به **AHM7** می‌داد. سرویس AHM7 فقط **یک** `videoUrl`
برمی‌گردونه — برای کروسل یعنی اسلاید اول، به‌عنوان ویدیو، بدون هیچ دکمه عکسی.

**راه‌حل:**

1. **همه پست‌های `/p/` حالا مثل لینک‌های `img_index` مستقیم به Apify می‌رن** (`is_instagram_post_page`
   جدید در `routing.py` + شرط `use_ahm7` در `bot.py`). اکتور `instagram-scraper` همه
   اسلایدها رو از `childPosts` برمی‌گردونه و ربات همه رو به‌صورت **آلبوم کامل و مرتب** می‌فرسته.
2. **منوی Apify برای `/p/` حالا هر سه دکمه رو داره:**
   - 🎬 ویدیو (کیفیت اصلی)
   - 📷 **تصاویر (همه اسلایدها)** ← دکمه جدید
   - 🎵 فقط صدا (MP3)
   حتی اگه کاربر برای یک کروسل تصویری «ویدیو» رو بزنه، باز هم همه عکس‌ها به‌صورت آلبوم
   میاد (اکتور هرچی پست واقعاً داشته باشه برمی‌گردونه).
3. **ریلز دست‌نخورده:** `/reel/` و `/reels/` و `/tv/` همچنان مسیر اصلی AHM7 رو دارن
   (همراه با پیشنهاد موزیک ریلز)، چون همیشه تک‌ویدیو هستن.
4. لینک‌های `img_index` هم مثل قبل فقط دکمه «📷 تصاویر (کیفیت اصلی)» رو دارن.

**فایل‌های این به‌روزرسانی:** `routing.py`، `bot.py`، `apify_gateway.py`

---

## 🔁 به‌روزرسانی ۳ — دانلود آخرین پست حالا با موتور خود ربات انجام می‌شه

**قبلاً:** دکمه «🖼 دانلود آخرین پست» فایل‌ها رو **مستقیم از لینک‌های CDN** که CreatorCrawl
برمی‌گردوند دانلود می‌کرد (مسیر جدا از موتور اصلی ربات).

**الان:** با زدن دکمه، ربات **لینک آخرین پست رو می‌گیره و انگار خود کاربر همون لینک رو
فرستاده**، وارد **موتور دانلود فعلی ربات** می‌شه:

- همون مسیرهای همیشگی: AHM7 → Apify → ربات‌های دانلودر تلگرامی → yt-dlp / SOCIAL_GATEWAY
- همون منوی کیفیت (وقتی سرویس گزینه بده)، همون کارت‌های پیشرفت، همون کپشن‌ها
- حتی پیشنهاد موزیک ریل (reel-music) هم مثل قبل کار می‌کنه

یعنی CreatorCrawl فقط **کشف** لینک رو انجام می‌ده (کارت پروفایل + لینک آخرین پست) و
هیچ نقشی در دانلود نداره. کد دانلود مستقیم CDN هم به‌طور کامل حذف شد.

**نکته:** چون دانلود دیگه به لیست `media` پاسخ API وابسته نیست، دکمه دانلود حالا هر وقت
**لینک آخرین پست** موجود باشه نمایش داده می‌شه (قبل‌تر اگر پارسر مدیا خالی برمی‌گردوند
دکمه اصلاً نشون داده نمی‌شد).

بقیه رفتار (کارت پروفایل، چرخش کلید، پی‌وی ادمین، شمارنده‌های Upstash) دست‌نخورده است.

---

## 🔁 به‌روزرسانی ۲ — رفع کامل باگ «❌ پیج پیدا نشد» (کردیت خورده ولی جواب نمی‌داد)

**علت باگ:** پارسر قبلی شکل پاسخ API را اشتباه حدس زده بود. طبق مستندات رسمی
CreatorCrawl (OpenAPI: `app.creatorcrawl.com/api/openapi.json`) پاسخ‌ها «نرمالایز‌شده»
هستند:

- `/api/instagram/profile` → `data: {handle, name, avatar_url, follower_count, post_count, verified, is_private, recent_posts[]}`
- `/api/instagram/user/posts` → `data: [ {url, type, text, media[], like_count, comment_count, author{}} ]`

پارسر قدیمی دنبال کلیدهای `user` و `items` بود که **اصلاً در پاسخ وجود ندارند**؛ برای
همین همه‌چیز — حتی پیج معروفی مثل `nike` — به «پیج پیدا نشد» می‌خورد و یک کردیت هم
مصرف می‌شد.

**چه چیزهایی درست شد:**

1. **پارسر با شکل واقعی پاسخ بازنویسی شد** (هر دو endpoint) + fallback برای شکل‌های قدیمی.
2. **مصرف کردیت هوشمند شد:** حالا اول `/instagram/profile` صدا زده می‌شه که عکس پروفایل +
   فالوور + تعداد پست + آخرین پست (`recent_posts`) رو با هم داره → معمولاً **کل کارت با ۱ کردیت** ساخته می‌شه.
   فقط اگه اون پاسخ پست قابل‌دانلود نداشت، یک کردیت دیگه صرف `/user/posts` می‌شه. پیج خصوصی اصلاً دومی رو صدا نمی‌زنه.
3. **«پیج پیدا نشد» دیگه دروغ نمی‌گه:** فقط برای ۴۰۰/۴۰۴ واقعی یا پاسخ خالی صریح. اگه شکل
   پاسخ ناشناخته باشه، خطای صادقانه نمایش داده می‌شه و **کل محتوای پاسخ در لاگ Railway
   چاپ می‌شه** تا هر ناسازگاری آینده یک‌نگاهی قابل دیباگ باشه.
4. **نقشه خطاها دقیق شد:** ۴۰۱/۴۰۲/۴۰۳/۴۲۹ = مشکل کلید → سوییچ خودکار + پیام به ادمین؛
   ۴۰۰/۴۰۴ = مشکل نام‌کاربری؛ 5xx/شبکه = خطای گذرا و retry روی کلید بعدی.
5. پیج خصوصی: در کپشن «🔒 این پیج خصوصی است» نمایش داده می‌شه.

بقیه رفتار (چرخش کلید، پی‌وی ادمین با ایمیل مالک کلید، شمارنده‌های ماندگار در Upstash)
دست‌نخورده و سالم باقی مانده است.

---

## چه چیزی عوض شد؟

متد قبلی (اسکرپ مستقیم اینستاگرام با `instagram_profile.py`) **کاملاً حذف شد** و جاش
**CreatorCrawl API** نشسته:

- Endpoint: `GET https://app.creatorcrawl.com/api/instagram/user/posts?handle=<username>`
- هدر: `x-api-key: <KEY>`
- هر درخواست موفق = ۱ credit (پلن رایگان هر اکانت = ۵۰ credit)

## رفتار جدید دستور `/profile <یوزرنیم>`

1. عکس پروفایل اکانت (از پاسخ API) ارسال می‌شه.
2. کپشن زیر عکس شامل: نام و یوزرنیم ✔️، **تعداد فالوور**، **تعداد پست**، **لینک آخرین پست**، لایک/کامنت آخرین پست و متن کپشنشه.
3. یک **دکمه «🖼 دانلود آخرین پست»** زیر عکس هست؛ با زدنش، ربات **لینک آخرین پست رو مثل
   یک لینک معمولی وارد موتور دانلود خودش می‌کنه** (همون مسیری که خود کاربر لینک رو
   می‌فرستاد: AHM7 → Apify → ربات‌های دانلودر → yt-dlp) — با منوی کیفیت، کارت‌های
   پیشرفت و کپشن‌های همیشگی ربات. CreatorCrawl فقط لینک رو پیدا می‌کنه.
4. یک دکمه «🔗 مشاهده پست در اینستاگرام» هم برای باز کردن خود پست اضافه شد.

## چرخش کلیدها (چند اکانت CreatorCrawl)

- `CREATORCRAWL_API_KEYS` لیست کلیدهاست، جدا شده با کاما، هر آیتم به شکل `KEY|EMAIL`
  (ایمیل = ایمیلی که باهاش اون اکانت CreatorCrawl رو ساختی):
  ```
  CREATORCRAWL_API_KEYS=KEY1|email1@gmail.com,KEY2|email2@gmail.com,KEY3|email3@gmail.com
  ```
- وقتی یک کلید به سقف `CREATORCRAWL_KEY_LIMIT` (پیش‌فرض ۵۰) برسه:
  - بلافاصله و شفاف روی **کلید بعدی** سوییچ می‌شه (کاربر هیچ وقفه‌ای نمی‌بینه).
  - یک **پیام به پی‌وی ادمین** (`BOT_ADMIN_CHAT_ID`) میاد و **ایمیل مالک اون کلید** +
    «۵۰ درخواست با این کلید زده شده» + کلیدی که روش سوییچ شده رو می‌گه.
- اگه API خودش کلید رو رد کنه (سهمیه/۴۰۲/۴۲۹/۴۰۱)، کلید همون‌جا مصرف‌شده علامت می‌خوره
  و درخواست **روی کلید بعدی خودکار retry** می‌شه (self-healing).
- اگه همه کلیدها پر بشن، پیام واضح «کلید جدید اضافه کن» به کاربر میاد.

## ماندگاری شمارنده‌ها (ری‌دیپلوی / ری‌استارت / اکانت جدید Railway)

شمارنده‌ی مصرف هر کلید بیرون از Railway نگه‌داری می‌شه:

- **بهترین راه — Upstash Redis REST (رایگان):** این دو متغیر رو در Railway بذار:
  ```
  UPSTASH_REDIS_REST_URL=https://xxxx.upstash.io
  UPSTASH_REDIS_REST_TOKEN=xxxx
  ```
  (در upstash.io یک دیتابیس رایگان بساز؛ از تب REST API مقدار URL و TOKEN رو کپی کن.)
  با این حالت: ری‌استارت، ری‌دیپلوی و حتی دیپلوی روی **اکانت Railway دیگه** هم
  شمارنده‌ها می‌مونن، چون تو خود Upstash ذخیره می‌شن.
- **بدون Upstash:** به‌صورت خودکار فایل محلی `cc_usage.json` استفاده می‌شه که فقط
  ری‌استارت رو دوام میاره و با ری‌دیپلوی صفر می‌شه (در لاگ هشدارش میاد).
- کلیدها با هش SHA-256 (نه مقدار واقعی کلید) در KV شناسایی می‌شن؛ پس تغییر/افزودن کلید
  در env هم شمارنده‌های بقیه رو به‌هم نمی‌ریزه.

## متغیرهای env که باید ست کنی

| متغیر | توضیح |
|---|---|
| `CREATORCRAWL_API_KEYS` | `KEY1|email1,KEY2|email2,...` (الزامی برای `/profile`) |
| `BOT_ADMIN_CHAT_ID` | آیدی عددی خودت برای پی‌وی اعلان پرشدن کلیدها |
| `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` | برای ماندگاری دائمی شمارنده‌ها |
| `CREATORCRAWL_KEY_LIMIT` | اختیاری؛ پیش‌فرض ۵۰ |

## فایل‌های تغییرکرده / جدید

| فایل | وضعیت |
|---|---|
| `creatorcrawl.py` | 🆕 جدید — کلاینت API + چرخش کلید + KV ماندگار + اعلان |
| `bot.py` | ✏️ بازنویسی `/profile` و کال‌بک دانلود؛ حذف متد قدیمی |
| `config.py` | ✏️ متغیرهای جدید |
| `.env.example` | ✏️ مستندسازی متغیرهای جدید |
| `Dockerfile` | ✏️ `creatorcrawl.py` اضافه، `instagram_profile.py` حذف |
| `README.md` | ✏️ به‌روزرسانی توضیحات |
| `instagram_profile.py` | 🗑 حذف شده — این فایل رو از ریپو/سرور خودت هم پاک کن |
