# نصب نسخه ۳ (v3 Fix Pack) — فقط ۶ فایل تغییر یافته

تغییرات این دور:
1. اسپاتیفای → Actor جدید `maximedupre/spotify-downloader`
   (Actor پیشنهادی musicae بررسی شد؛ فقط metadata می‌دهد نه فایل کامل.
   Actor جدید: لینک مستقیم MP3 + حجم دقیق + متادیتای کامل)
2. پینترست → منو هوشمند: پین تصویری فقط گزینه تصویر، پین ویدیویی فقط ویدیو (تشخیص با og:video)
3. هوش مصنوعی دقیق‌تر: FAQ امتیازدهی دومطبقه + ۱۰ موضوع جدید؛
   سوالات نامشخص با کانتکست کامل ربات به مدل می‌روند؛
   HuggingFace روی endpoint جدید router + زنجیره fallback مدل
4. تست سلامت: بعد از هر استارت گزارش کامل به PV مدیر + دستور /health

نصب: محتوا را روی ریشه‌ی ریپو extract کنید و deploy بگیرید.
هیچ متغیر جدیدی لازم نیست (فقط BOT_ADMIN_CHAT_ID برای دریافت PV سلامت).
تست: ۲۷/۲۷ تست قبلی پاس؛ ۹ سناریوی جدید پاس.
